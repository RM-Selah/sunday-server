{
  "name": "sunday-api",
  "version": "3.0.0",
  "description": "Sunday — Worship Team Assistant API (Cloudflare Worker)",
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "set-key": "wrangler secret put ANTHROPIC_API_KEY",
    "start:legacy": "node legacy/sunday-server.js"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
